# 核心依赖
loguru>=0.7.2
typing-extensions>=4.0.0

# 向量数据库和搜索 - 兼容版本
chromadb>=0.4.15
sentence-transformers>=2.2.2
numpy>=1.21.0
pandas>=1.3.0

# 树形语法分析 - 更新到兼容版本
tree-sitter>=0.20.0
tree-sitter-languages>=1.7.0

# 代码分析工具
clang>=15.0.0  # Python bindings for clang
lizard>=1.17.0  # 代码复杂度分析
pyyaml>=6.0  # YAML配置文件支持

# 成熟开源工具集成 - Windows专用
# 注意：静态分析主要依赖外部工具，而非Python包
# Windows: 使用Cppcheck + Clang Static Analyzer + 其他Windows兼容工具

# Python代码分析工具（跨平台）
bandit>=1.7.0  # Python安全分析
flake8>=6.0.0  # Python代码质量检查

# LSP和协议支持
python-lsp-server>=1.7.0  # Python LSP服务器
pygls>=1.3.0  # LSP客户端异步支持
lsprotocol>=2023.0.1  # LSP协议实现
xmltodict>=0.13.0  # XML解析支持（用于Cppcheck输出）

# Web服务器和API - 更新到兼容版本
fastapi>=0.115.0  # 兼容mcp-feedback-enhanced
uvicorn>=0.31.1  # 兼容mcp
python-multipart>=0.0.9  # 兼容mcp
anyio>=4.5  # 兼容mcp
rich>=14.0.0  # 兼容多个包

# Windows兼容性
pywin32>=306; sys_platform == "win32"
colorama>=0.4.6; sys_platform == "win32"

# 可选依赖（用于高级功能）
# 取消注释以下行来启用额外功能：

# SonarQube集成（需要Java）
# sonar-scanner>=1.0.0

# Git分析
# gitpython>=3.1.0

# 更多语言支持
# javalang>=0.13.0  # Java解析
# esprima>=4.0.0    # JavaScript解析
# ast>=3.0.0        # Python AST分析

# 机器学习和AI增强
# transformers>=4.21.0
# torch>=1.12.0

# 数据可视化
# matplotlib>=3.5.0
# plotly>=5.0.0

# 开发和测试
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=22.0.0
mypy>=1.0.0
pre-commit>=2.20.0 