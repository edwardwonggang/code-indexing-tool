# 核心依赖
loguru==0.7.2
typing-extensions>=4.0.0

# 向量数据库和搜索
chromadb==0.4.15
sentence-transformers==2.2.2
numpy>=1.21.0
pandas>=1.3.0

# 树形语法分析
tree-sitter>=0.20.0
tree-sitter-languages>=1.7.0

# 代码分析工具
clang>=15.0.0  # Python bindings for clang
lizard>=1.17.0  # 代码复杂度分析
pyyaml>=6.0  # YAML配置文件支持

# 新增：成熟开源工具集成
semgrep>=1.45.0  # Semgrep静态分析工具
python-lsp-server>=1.7.0  # Python LSP服务器
asyncio-lsp>=0.3.0  # LSP客户端异步支持
xmltodict>=0.13.0  # XML解析支持（用于Cppcheck输出）

# Web服务器和API
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Windows兼容性
pywin32>=306; sys_platform == "win32"
colorama>=0.4.6; sys_platform == "win32"

# 可选依赖（用于高级功能）
# 取消注释以下行来启用额外功能：

# SonarQube集成（需要Java）
# sonar-scanner>=1.0.0

# Git分析
# gitpython>=3.1.0

# 更多语言支持
# javalang>=0.13.0  # Java解析
# esprima>=4.0.0    # JavaScript解析
# ast>=3.0.0        # Python AST分析

# 机器学习和AI增强
# transformers>=4.21.0
# torch>=1.12.0

# 数据可视化
# matplotlib>=3.5.0
# plotly>=5.0.0

# 开发和测试
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=22.0.0
mypy>=1.0.0
pre-commit>=2.20.0 